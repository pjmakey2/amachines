# Django Settings
SECRET_KEY=cambiar-en-produccion-usar-clave-segura
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=amachine
DB_USER=amachine
DB_PASSWORD=cambiar-password-seguro
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Celery
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# SIFEN
SIFEN_KEY_PASS=tu-clave-sifen

# Email (Mailgun API)
MAILGUN_API_KEY=tu-api-key-mailgun
MAILGUN_DOMAIN=tu-dominio-mailgun.com
DEFAULT_FROM_EMAIL=tu-email@dominio.com

# Sentry
SENTRY_DSN=tu-dsn-sentry

# Domain (for production)
FDOMAIN=http://localhost:8000

# CSRF Trusted Origins (required for Django 4.0+ behind proxy)
# Must include the full URL with protocol and port
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
